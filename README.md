# Acumetis Sustainability in Action

Upload `index.html`, `styles.css`, and `app.js` to the root of a new public GitHub repository.

Enable GitHub Pages from **Settings → Pages → Deploy from a branch → main → /(root)**.

Embed the resulting Pages URL in 360Learning using an iframe.
